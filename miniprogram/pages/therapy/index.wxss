/* pages/therapy/index.wxss */

.container {
  margin-top: calc(44px + env(safe-area-inset-top));
  padding: 0; /* 移除padding，让组件自己处理 */
  background-color: #f8f9fa;
  min-height: calc(100vh - 44px - env(safe-area-inset-top) - 98rpx); /* 减去tabBar高度 */
  padding-bottom: 98rpx; /* 为tabBar留出空间 */
}

.quiz-question {
  width: 100%;
  height: 100%;
}