<view class="profile-container">
  <!-- 用户信息卡片 -->
  <view class="user-card">
    <view class="user-avatar">
      <image class="avatar-image" src="{{userInfo.avatarUrl || '/images/icons/avatar.png'}}" mode="aspectFill"></image>
    </view>
    <view class="user-info">
      <text class="user-name">{{userInfo.nickName || '用户'}}</text>
      <text class="user-level">疼痛管理 {{userLevel}} 级</text>
    </view>
    <view class="edit-button" bindtap="editProfile">
      <text class="edit-text">编辑</text>
    </view>
  </view>

  <!-- 数据统计 -->
  <view class="stats-section">
    <view class="stats-title">我的数据</view>
    <view class="stats-grid">
      <view class="stat-item">
        <text class="stat-number">{{userStats.totalDays}}</text>
        <text class="stat-label">使用天数</text>
      </view>
      <view class="stat-item">
        <text class="stat-number">{{userStats.assessments}}</text>
        <text class="stat-label">评估次数</text>
      </view>
      <view class="stat-item">
        <text class="stat-number">{{userStats.completedCourses}}</text>
        <text class="stat-label">完成课程</text>
      </view>
      <view class="stat-item">
        <text class="stat-number">{{userStats.mindfulnessMinutes}}</text>
        <text class="stat-label">正念时长(分)</text>
      </view>
    </view>
  </view>

  <!-- 疼痛趋势图表 -->
  <view class="chart-section">
    <view class="chart-header">
      <text class="chart-title">疼痛趋势</text>
      <view class="chart-tabs">
        <text class="chart-tab {{chartPeriod === '7d' ? 'active' : ''}}" bindtap="switchChartPeriod" data-period="7d">7天</text>
        <text class="chart-tab {{chartPeriod === '30d' ? 'active' : ''}}" bindtap="switchChartPeriod" data-period="30d">30天</text>
      </view>
    </view>
    <view class="chart-container">
      <canvas class="pain-chart" canvas-id="painTrendChart" id="painTrendChart"></canvas>
    </view>
  </view>

  <!-- 成就徽章 -->
  <view class="achievements-section">
    <view class="section-title">我的成就</view>
    <view class="achievements-grid">
      <view class="achievement-item {{item.unlocked ? 'unlocked' : 'locked'}}" 
            wx:for="{{achievements}}" wx:key="id">
        <view class="achievement-icon">{{item.icon}}</view>
        <text class="achievement-name">{{item.name}}</text>
        <text class="achievement-desc">{{item.description}}</text>
      </view>
    </view>
  </view>

  <!-- 功能菜单 -->
  <view class="menu-section">
    <view class="menu-item" bindtap="exportData">
      <view class="menu-icon">📊</view>
      <view class="menu-content">
        <text class="menu-title">数据导出</text>
        <text class="menu-desc">导出您的疼痛管理数据</text>
      </view>
      <view class="menu-arrow">></view>
    </view>

    <view class="menu-item" bindtap="remindSettings">
      <view class="menu-icon">⏰</view>
      <view class="menu-content">
        <text class="menu-title">提醒设置</text>
        <text class="menu-desc">设置评估和训练提醒</text>
      </view>
      <view class="menu-arrow">></view>
    </view>

    <view class="menu-item" bindtap="privacySettings">
      <view class="menu-icon">🔒</view>
      <view class="menu-content">
        <text class="menu-title">隐私设置</text>
        <text class="menu-desc">管理您的数据隐私</text>
      </view>
      <view class="menu-arrow">></view>
    </view>

    <view class="menu-item" bindtap="aboutApp">
      <view class="menu-icon">ℹ️</view>
      <view class="menu-content">
        <text class="menu-title">关于应用</text>
        <text class="menu-desc">版本信息和使用帮助</text>
      </view>
      <view class="menu-arrow">></view>
    </view>

    <view class="menu-item" bindtap="feedback">
      <view class="menu-icon">💬</view>
      <view class="menu-content">
        <text class="menu-title">意见反馈</text>
        <text class="menu-desc">帮助我们改进应用</text>
      </view>
      <view class="menu-arrow">></view>
    </view>
  </view>

  <!-- 退出登录 -->
  <view class="logout-section">
    <button class="logout-btn" bindtap="logout">退出登录</button>
  </view>
</view>